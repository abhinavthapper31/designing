package lowLevelDesign.designPatterns.proxy;

public class App {

    /*
     * Uses :
     *
     * eg visiting website via proxy, request to net go via proxy (blocking blacklisted websites)
     * Pre and Post processing ()
     * Caching (access to real object does not happen, but first cache is fetched)
     * Access restriction
     *
     *
     * */
    public static void main(String[] args) {

    }
}
